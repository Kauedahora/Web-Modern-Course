// Variável constante chamada potencia, que recebe dois numeros por parâmetro armazenados em uma função arrow que calcula a potencia.
const potencia = (base, expoente) => Math.pow(base, expoente);  //Math.pow() - retorna a base elevada ao expoente!
console.log(potencia(5, 2));